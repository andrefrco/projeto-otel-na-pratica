apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - kubernetes.yaml
configMapGenerator:
  - name: otelcol
    files:
      - config.yaml=otelcol.yaml
generatorOptions:
  disableNameSuffixHash: true
